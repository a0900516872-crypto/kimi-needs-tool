# 需求挖掘工具 — 录音转写 + 智能汇总 功能方案

> 状态：待批注  
> 基于 `_研究.md` 代码分析，方案定稿后执行。

---

## 一、目标与范围

### 目标
把「需求挖掘工具」从纯演示升级为客户访谈工作流工具：
1. **实时录音转写**：每页问题旁一键录音，客户说话实时转成文字
2. **结构化汇总**：访谈结束后一键生成按章节整理的需求摘要，可直接复制/导出

### 范围（做什么）
- 仅问题页（`type === 'question'`）启用录音
- 使用浏览器原生 Web Speech API，零后端、零成本
- 答案和笔记系统共存：笔记是设计师自由发挥，答案是客户原话
- 汇总页在 ReviewSlide 实现，替代现有静态标签

### 不做什么（边界）
- 不替代现有笔记面板（`dm_notes` 保留）
- 不做 AI 提炼/摘要（当前只汇总原文，后续可升级）
- 不做音频文件保存（只保留文字，不存录音文件）
- 不改造非问题页的幻灯片类型

---

## 二、功能设计

### 2.1 录音转写（QuestionSlide）

**交互流程**：
```
进入问题页
  ↓
页面下方显示：🎤 点击麦克风开始录音（或显示已有答案）
  ↓
点击麦克风 → 按钮红色脉冲闪烁 → 实时文字逐字出现
  ↓
再次点击 → 停止录音 → 文字变为可编辑状态
  ↓
翻页或手动保存 → 写入 localStorage
```

**UI 元素**：
| 元素 | 位置 | 说明 |
|------|------|------|
| 麦克风按钮 | 问题标题右下方 | 圆形，空闲灰色/录音红色脉冲 |
| 实时文字区 | 引导语下方 | 浅灰底色，逐字显示转写内容 |
| 编辑区 | 实时文字区下方 | textarea，可手动修改 |
| 保存提示 | 编辑区右下角 | "已保存" 淡入淡出 |
| 隐私提示 | 编辑区下方 | 「文字仅保存在本地，不会上传」|

**不支持 Speech API 的浏览器降级**：
- 隐藏麦克风按钮
- 直接显示 textarea：「当前浏览器不支持语音输入，请手动输入客户回答」

### 2.2 智能汇总（ReviewSlide）

**改造后 ReviewSlide 结构**：
```
标题：关键信息回顾
  ↓
按章节分组展示（5个章节卡片）
  ├─ 你和你的家
  │   ├─ Q1: 家里住几口人？
  │   │   → [客户回答文字...]
  │   ├─ Q2: 这次为什么换房？
  │   │   → [客户回答文字...]
  │   └─ ...
  ├─ 一天的节奏
  │   └─ ...
  └─ ...
  ↓
底部操作栏：复制全部 / 导出 Markdown
```

**空状态**：某题未录音 → 显示「未记录」，灰色斜体

### 2.3 快捷入口（Controls）

底部控制栏右侧新增「汇总」按钮：
- 图标：ClipboardList
- 仅在有答案时高亮（`accent` 色）
- 点击直接跳转到 ReviewSlide（即汇总页）
- 悬浮提示：`Alt+R` 快捷键

---

## 三、数据设计

### 3.1 答案数据结构

```typescript
// src/types/answers.ts  (新建)
export interface Answer {
  slideIndex: number;   // 页码索引（与 SLIDES 数组对齐）
  question: string;     // 问题标题（冗余，方便汇总展示）
  chapter: string;      // 章节编号 1-5
  transcript: string;   // 转写/输入的文字
  updatedAt: string;    // ISO 时间戳
}

export type AnswersMap = Record<number, Answer>;
```

### 3.2 localStorage

| Key | 数据 | 说明 |
|-----|------|------|
| `dm_notes` | `Record<number, string>` | 现有设计师笔记，不动 |
| `dm_answers` | `Record<number, Answer>` | 新增客户答案 |

### 3.3 App.tsx 状态扩展

```typescript
const [answers, setAnswers] = useState<AnswersMap>({});

// 加载
useEffect(() => {
  try {
    const raw = localStorage.getItem('dm_answers');
    if (raw) setAnswers(JSON.parse(raw));
  } catch { /* ignore */ }
}, []);

// 保存
const saveAnswer = useCallback((slideIndex: number, transcript: string) => {
  const slide = SLIDES[slideIndex];
  const next: AnswersMap = {
    ...answers,
    [slideIndex]: {
      slideIndex,
      question: slide.title || '',
      chapter: slide.chapter || '',
      transcript,
      updatedAt: new Date().toISOString(),
    }
  };
  setAnswers(next);
  localStorage.setItem('dm_answers', JSON.stringify(next));
}, [answers]);
```

---

## 四、文件清单

| # | 路径 | 操作 | 改动说明 |
|---|------|------|----------|
| 1 | `src/hooks/useSpeechRecognition.ts` | **新建** | 封装 Web Speech API，暴露 `isListening/transcript/start/stop/supported` |
| 2 | `src/types/answers.ts` | **新建** | `Answer` 和 `AnswersMap` 类型定义 |
| 3 | `src/App.tsx` | 修改 | 新增 `answers` 状态、`saveAnswer` 回调，传给 QuestionSlide 和 ReviewSlide |
| 4 | `src/components/slides/QuestionSlide.tsx` | **重写** | 加录音按钮、实时转写区、编辑区、保存逻辑 |
| 5 | `src/components/slides/ReviewSlide.tsx` | **重写** | 从静态4个标签改成交互式按章节汇总页，加复制/导出 |
| 6 | `src/components/Controls.tsx` | 修改 | 右侧加「汇总」跳转按钮 |
| 7 | `src/components/NavigationMenu.tsx` | 可选修改 | 菜单项加汇总入口 |
| 8 | `src/index.css` | 修改 | 加 `.recorder-pulse` 动画、转写文字区样式 |
| 9 | `src/App.css` | 可选修改 | 如有需要微调 |

---

## 五、关键代码片段

### 5.1 Speech Recognition Hook（核心）

```typescript
// src/hooks/useSpeechRecognition.ts
import { useState, useRef, useCallback, useEffect } from 'react';

export function useSpeechRecognition() {
  const [isListening, setIsListening] = useState(false);
  const [transcript, setTranscript] = useState('');
  const [interim, setInterim] = useState('');
  const recognitionRef = useRef<any>(null);

  const supported = typeof window !== 'undefined' &&
    ('SpeechRecognition' in window || 'webkitSpeechRecognition' in window);

  useEffect(() => {
    if (!supported) return;
    const SR = (window as any).SpeechRecognition || (window as any).webkitSpeechRecognition;
    const r = new SR();
    r.lang = 'zh-CN';
    r.continuous = true;
    r.interimResults = true;

    r.onresult = (e: any) => {
      let final = '';
      let temp = '';
      for (let i = e.resultIndex; i < e.results.length; i++) {
        if (e.results[i].isFinal) final += e.results[i][0].transcript;
        else temp += e.results[i][0].transcript;
      }
      if (final) setTranscript(prev => prev + final);
      setInterim(temp);
    };

    r.onend = () => {
      // 如果是主动停止的，不自动重启
      if (isListening) {
        // 意外中断时自动续录（Chrome 60s 限制）
        try { r.start(); } catch { /* already started */ }
      }
    };

    recognitionRef.current = r;
    return () => { try { r.stop(); } catch {} };
  }, [supported]);

  const start = useCallback(() => {
    setTranscript('');
    setInterim('');
    setIsListening(true);
    try { recognitionRef.current?.start(); } catch {}
  }, []);

  const stop = useCallback(() => {
    setIsListening(false);
    setInterim('');
    try { recognitionRef.current?.stop(); } catch {}
  }, []);

  return { supported, isListening, transcript, interim, start, stop };
}
```

### 5.2 QuestionSlide 录音区布局（核心 UI）

```tsx
// 引导语下方新增区域
<div className="mt-10 max-w-xl">
  {/* 录音按钮 + 状态 */}
  <div className="flex items-center gap-4 mb-4">
    <button
      onClick={isListening ? onStopRecording : onStartRecording}
      className={`w-12 h-12 rounded-full flex items-center justify-center transition-all ${
        isListening ? 'bg-red-600 animate-pulse' : 'bg-[var(--border-light)] hover:bg-[var(--border)]'
      }`}
    >
      <Mic size={20} className="text-white" />
    </button>
    <span className="text-[12px] text-[var(--text-muted)]">
      {isListening ? '正在聆听...' : supported ? '点击麦克风开始录音' : '当前浏览器不支持语音输入'}
    </span>
  </div>

  {/* 实时转写 / 编辑区 */}
  <div className="bg-[var(--bg-card)] border border-[var(--border)] rounded-sm p-4">
    <textarea
      className="w-full bg-transparent text-[14px] leading-relaxed text-[var(--text-secondary)] outline-none resize-none scrollbar-thin"
      rows={4}
      value={displayText}
      onChange={(e) => onEdit(e.target.value)}
      placeholder={supported ? "客户回答将显示在这里，可直接修改..." : "请手动输入客户回答..."}
    />
    <div className="flex justify-end mt-2">
      <span className="text-[10px] text-[var(--text-dim)]">
        {isListening ? interim || '聆听中...' : saveStatus}
      </span>
    </div>
  </div>
</div>
```

### 5.3 ReviewSlide 汇总布局（核心 UI）

```tsx
// 按章节分组渲染
const chapters = ['1','2','3','4','5'];
const chapterNames = { '1':'你和你的家', ... };

return (
  <div className="h-full overflow-y-auto scrollbar-thin px-8 py-12">
    <h2>关键信息回顾</h2>

    {chapters.map(ch => {
      const chapterAnswers = Object.values(answers).filter(a => a.chapter === ch);
      return (
        <div key={ch} className="mb-8 border border-[var(--border)] p-6">
          <h3 className="text-[var(--accent)] text-[12px] tracking-widest mb-4">
            {chapterNames[ch]}
          </h3>
          {chapterAnswers.map(a => (
            <div key={a.slideIndex} className="mb-4">
              <div className="text-[13px] font-medium text-[var(--text)]">{a.question}</div>
              <div className="text-[13px] text-[var(--text-secondary)] mt-1 pl-3 border-l border-[var(--border-light)]">
                {a.transcript || <span className="italic text-[var(--text-muted)]">未记录</span>}
              </div>
            </div>
          ))}
        </div>
      );
    })}

    {/* 操作栏 */}
    <div className="flex gap-4">
      <button onClick={copyAll}>复制全部</button>
      <button onClick={exportMd}>导出 Markdown</button>
    </div>
  </div>
);
```

### 5.4 导出 Markdown 格式

```markdown
# 飛计划 STUDIO — 客户需求挖掘访谈记录

访谈时间：2026-05-02

---

## 一、你和你的家

**Q1: 家里住几口人？**
> 客户回答文字...

**Q2: 这次为什么换房？**
> 客户回答文字...

---

## 二、一天的节奏

...
```

---

## 六、执行步骤

1. **新建类型和 Hook** → `src/types/answers.ts` + `src/hooks/useSpeechRecognition.ts`
2. **扩展 App.tsx 状态** → 加 `answers`、`saveAnswer`、localStorage 读写
3. **重写 QuestionSlide** → 录音按钮 + 转写区 + 编辑区
4. **重写 ReviewSlide** → 按章节汇总 + 复制/导出
5. **修改 Controls** → 加汇总跳转按钮
6. **加样式** → 脉冲动画、转写区样式
7. **本地测试** → Chrome 下录音测试、Safari 降级测试、汇总导出测试
8. **提交部署** → `git add . && git commit && git push` → Vercel 自动部署

---

## 七、依赖与风险

| 风险 | 概率 | 影响 | 应对 |
|------|------|------|------|
| Safari 不支持 Speech API | 高 | 苹果设备无法录音 | 降级为手动输入 textarea，不影响核心流程 |
| Chrome 60s 自动断录 | 中 | 长回答被截断 | `continuous: true` + `onend` 自动续录（代码已考虑）|
| 中文识别错别字 | 中 | 转写质量一般 | 提供实时编辑区，用户随时修改 |
| 麦克风权限被拒 | 低 | 无法录音 | 提示用户开启权限，同时支持手动输入 |
| localStorage 容量超限 | 极低 | 大量长文本存不下 | 单条答案限长 5000 字，超限截断提示 |

**零新增依赖**：不需要 npm install 任何包，全部用浏览器原生 API + 现有技术栈。

---

> 先别执行（don't implement yet）—— 等飞总批注定稿。
