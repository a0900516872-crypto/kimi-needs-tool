# 需求挖掘工具 — 录音转写功能研究

## 一、项目概述

**飛计划 STUDIO 需求挖掘工具**，一个面向室内设计客户访谈的 React 演示应用。
- 技术栈：Vite + React 19 + TypeScript + Tailwind CSS v3 + shadcn/ui
- 部署：Vercel（已绑定 GitHub 自动部署）
- 用途：设计师与客户面谈时，按幻灯片引导提问，记录客户回答

## 二、文件结构

```
src/
├── data/
│   ├── slides.ts          # 所有幻灯片数据（问题、章节、案例等）
│   └── cases.ts           # 案例配置（3个占位案例）
├── components/
│   ├── slides/            # 各类型幻灯片组件
│   │   ├── CoverSlide.tsx
│   │   ├── PhilosophySlide.tsx
│   │   ├── CaseSlide.tsx
│   │   ├── TransitionSlide.tsx
│   │   ├── ChapterIntroSlide.tsx
│   │   ├── QuestionSlide.tsx    ← 核心：需要加录音控件
│   │   ├── ReviewSlide.tsx      ← 核心：需要改成交互式汇总页
│   │   └── EndSlide.tsx
│   ├── Spotlight.tsx
│   ├── Header.tsx
│   ├── Footer.tsx
│   ├── NavigationMenu.tsx
│   ├── Controls.tsx             ← 底部控制栏，可加汇总按钮
│   ├── NotesPanel.tsx           ← 现有笔记面板（localStorage）
│   └── Timer.tsx
├── App.tsx                      ← 全局状态管理
├── index.css                    ← 样式变量和组件类
└── main.tsx
```

## 三、数据流与状态管理

### 3.1 幻灯片数据结构（slides.ts）

```typescript
interface SlideData {
  type: 'cover' | 'philosophy' | 'case' | 'transition' | 'chapter-intro' | 'question' | 'review' | 'end';
  act: number;
  chapter?: string;      // 1-5
  title?: string;        // 问题标题
  guide?: string;        // 引导语
  image?: string;
  // ...
}
```

**关键发现**：
- 共 **16 道问题**（chapter 1: 4题, chapter 2: 4题, chapter 3: 4题, chapter 4: 3题, chapter 5: 2题）
- 问题页 `type === 'question'`，通过 `slide.chapter` 关联章节
- `buildSlides()` 返回扁平数组，索引即页码

### 3.2 现有状态（App.tsx）

| 状态 | 类型 | 用途 |
|------|------|------|
| `current` | number | 当前幻灯片索引 |
| `showNotes` | boolean | 笔记面板开关 |
| `showMenu` | boolean | 导航菜单开关 |
| `noteCount` | number | 有内容的笔记页数 |
| `started` | boolean | 是否已开始（跳过封面）|
| `slideKey` | number | 强制重新渲染动画 |

**导航**：键盘(←→↑↓空格)、触摸滑动、滚轮、底部按钮。

### 3.3 笔记系统（NotesPanel.tsx）

- 存储 key：`localStorage.getItem('dm_notes')`
- 数据结构：`Record<number, string>` —— 页码索引 → 笔记内容
- 每页独立 textarea，800ms 防抖自动保存
- 支持导出 Markdown：`访谈笔记_YYYY-MM-DD.md`
- 快捷键：`N` 开关面板，`Ctrl+Enter` 手动保存

**关键发现**：笔记系统是按「页码索引」存储的，不是按「问题ID」。由于问题页和非问题页混在一起，需要单独维护一套「问题答案」数据。

## 四、录音功能技术方案

### 4.1 方案选择：Web Speech API（浏览器原生）

```typescript
const recognition = new (window.SpeechRecognition || window.webkitSpeechRecognition)();
recognition.lang = 'zh-CN';
recognition.continuous = true;      // 持续监听
recognition.interimResults = true;  // 返回临时结果（实时显示）
```

**优点**：零成本、零配置、实时转写、无需后端
**局限**：
- Chrome/Edge 支持最好，Safari/Firefox 不支持
- 中文识别准确度中等（访谈场景够用）
- 需要 HTTPS 或 localhost（Vercel 已满足）
- 单次最长 60 秒（`continuous: true` 可续）

**降级策略**：不支持 Speech API 的浏览器，显示「请手动输入」提示。

### 4.2 数据存储设计

新增 localStorage key：`dm_answers`

```typescript
interface AnswerData {
  slideIndex: number;      // 页码索引
  question: string;        // 问题标题（冗余存储，方便汇总）
  chapter: string;         // 章节编号
  transcript: string;      // 转写/输入的文字
  updatedAt: string;       // ISO 时间戳
}

// localStorage: 'dm_answers' → Record<number, AnswerData>
```

与现有笔记系统（`dm_notes`）**共存不冲突**：
- `dm_notes`：设计师自由笔记（每页一个）
- `dm_answers`：客户回答（仅问题页，结构化）

## 五、需要修改的文件清单

| # | 文件 | 操作 | 改动内容 |
|---|------|------|----------|
| 1 | `src/App.tsx` | 修改 | 新增 answers 状态，传给 QuestionSlide 和 ReviewSlide |
| 2 | `src/components/slides/QuestionSlide.tsx` | 修改 | 加录音按钮、实时转写显示、答案编辑区 |
| 3 | `src/components/slides/ReviewSlide.tsx` | 重写 | 从静态标签改成交互式汇总页，按章节展示所有答案 |
| 4 | `src/components/Controls.tsx` | 修改 | 加「查看汇总」快捷按钮 |
| 5 | `src/components/NotesPanel.tsx` | 可选修改 | 加答案预览或保持独立 |
| 6 | `src/index.css` | 修改 | 加录音按钮样式、转写文字样式、脉冲动画 |
| 7 | `src/data/slides.ts` | 可选修改 | 给每个 question 加唯一 id（方便映射）|

## 六、关键交互细节

1. **录音触发**：QuestionSlide 右下角加圆形麦克风按钮，点击开始/停止
2. **实时反馈**：录音时按钮红色脉冲，转写文字实时出现在问题下方
3. **答案编辑**：转写结果可手动修改（客户可能说错或需要精简）
4. **自动保存**：停止录音后自动保存到 localStorage
5. **汇总入口**：
   - Controls 底部加「汇总」按钮（仅在所有问题都有答案后高亮）
   - ReviewSlide 显示结构化汇总（按章节分组）
   - 支持复制为 Markdown / 导出
6. **数据持久**：刷新页面不丢失，关闭浏览器再开还在

## 七、风险与限制

| 风险 | 影响 | 应对 |
|------|------|------|
| Safari 不支持 Speech API | 苹果设备无法录音 | 降级为手动输入textarea |
| 识别准确度不高 | 转写文字有错别字 | 提供实时编辑区，用户可改 |
| 60秒超时 | 长回答被截断 | continuous 模式自动续，或提示分段 |
| 隐私顾虑 | 客户介意录音 | 页面上明确提示「仅本地转写，不上传服务器」|

